# Deployment dependency list.
#
# pyproject.toml stays the source of truth for local development
# (`pip install -e ".[dev]"`). This file exists because Railpack/Nixpacks and
# most PaaS builders will not install from a bare setuptools pyproject with no
# lock file — they detect Python, skip the install step entirely, and the
# container then dies on `uvicorn: not found`.
#
# Keep the two in sync when adding a dependency.

fastapi>=0.111
uvicorn[standard]>=0.30
pandas>=2.2
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
rapidfuzz>=3.9
openpyxl>=3.1
python-dotenv>=1.0
python-multipart>=0.0.9
typer>=0.12
